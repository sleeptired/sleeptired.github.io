---
title: "2026-03-12 TIL (13일차)"
date: 2026-03-12 08:00:00 +0900
categories: ["TIL(Today I Learned)", "2026-03-12"]
tags: [TIL, "2026", 내일배움캠프, C++]
---

> ## 💡 [Pro-Tip] C++ 메모리 누수(Memory Leak) 잡아내기
>
> 12일차에 배웠던 메모리 누수를 더 쉽게 잡아내는 코드  
> 동적 할당이 많은 게임 개발에서 메모리 누수는 치명적입니다. 아래 코드를 `main` 함수 최상단에 넣으면 프로그램 종료 시 출력 창에 누수 여부를 친절하게 알려줍니다. **선택이 아닌 필수**입니다!  
> 
> ```cpp
> #include <crtdbg.h>
> 
> int main() {
>     // 프로그램 종료 시 메모리 누수 리포트를 출력창에 표시함
>     _CrtSetDbgFlag(_CRTDBG_ALLOC_MEM_DF | _CRTDBG_LEAK_CHECK_DF);
>     // 메모리 릭 추적.
>	    // _CrtSetBreakAlloc(161);//디버그에 찍힌 블럭 번호를 입력
>		  // 메모리 릭 지점에 자동으로 중단점을 걸어주는 함수.
>     
>     /* 로직 시작 */
> }
> ```
{: .prompt-danger }

## **4번과제 필수기능**

### **1. 어떤 의도로 이 코드를 구현했을까?**  
![과제 기본 구조](/assets/img/Week2_assignments4_structure.png)  

> ● AlchemyWorkshop클래스가 PotionRecipe를 가지고 있어서 Has-A관계  
> ● PotionRecipe클래스는 물약 이름 + 여러가지 재료 목록을 담는 클래스(데이터)  
> ● AlchemyWorkshop클래스는 PotionRecipe들이 생성된걸 저장하여 관리하고 사용하는 클래스(매니저)  
{: .prompt-info }

### **2. 수정을 해야 한다면, 기존 코드의 수정을 최소화 할 수 있는 방안이 있을까?**
1) AlchemyWorkshop,PotionRecipe 파일 분할  

> **수정 사항**  
> ● 각 클래스 헤더파일에는 클래스 선언부만 남겨 인터페이스를 명확히 함  
> ● 각 클래스 cpp파일에는 세부 구현 로직을 분리하여 컴파일 속도 및 가독성 향상    
{: .prompt-info }

2) PotionRecipe 멤버 변수 캡슐화  
PotionRecipe 내 데이터가 외부에서 무분별하게 수정되는 것을 방지하기 위해 public 접근 제어자를 private으로 변경하고 캡슐화를 적용했습니다.  

```diff
 class PotionRecipe {
- public:
-    std::string potionName;
-    std::vector<std::string> ingredients;
+ private:
+    std::string potionName;
+    std::vector<std::string> ingredients;

+ public:
+    PotionRecipe(const std::string& name, const std::vector<std::string>& ingredients)
+        : potionName(name), ingredients(ingredients) {
+    }
 };
```
**멤버 변수를 private로 선언을 했기때문에 다른 곳에서 접근을 못하기 때문에 오류가 생겨 멤버 함수 get으로 값을 받아오게 구현**  
   
### **3. 객체지향 프로그래밍 언어라면 SOLID 원칙을 잘 준수하고 있나?**

1) SRP (단일 책임 원칙)
PotionRecipe: 레시피 데이터 정보만 전문적으로 담습니다.  
AlchemyWorkshop: 레시피들의 목록을 관리하는 로직에만 집중합니다.  

2) OCP (개방-폐쇄 원칙) 점검 및 해결

> **문제점**  
> 만약 PotionRecipe에 gold, time 같은 새로운 변수가 추가된다면?  
> ● addRecipe 함수의 매개변수를 매번 뜯어고쳐야 합니다.  
> ● 해당 함수를 호출하던 프로젝트 내의 **모든 코드**에서 에러가 발생하며 일일이 수정해야 합니다.  
{: .prompt-danger }

```diff
// AlchemyWorkshop.h
- void addRecipe(string name, vector<string> ingredients); 
+ // 완성된 객체 자체를 넘겨줌으로써 확장에 유연하게 대처
+ void addRecipe(const PotionRecipe& newRecipe);
```

> **해결 결과**  
> ● 유지보수성 향상: PotionRecipe 클래스에 어떤 변수가 추가되더라도 AlchemyWorkshop의 코드는 단 한 줄도 수정할 필요가 없습니다.  
> ● 코드 안정성: 유저의 입력을 받아 객체를 생성하는 main 함수 코드만 수정하면 되므로, 기존 매니저 클래스의 로직을 건드릴 위험이 사라집니다.   
{: .prompt-tip }

### **4.물약 이름으로 검색이 가능하게 하기**
물약의 이름으로 레시피를 찾는 검색 기능을 구현하면서, 반환형을 기존의 PotionRecipe (값 반환)에서 **PotionRecipe* (포인터 반환)**로 변경했습니다.

```diff
// AlchemyWorkshop.h
- PotionRecipe FindRecipe(const std::string& name); 
+ // 검색 결과가 없을 경우를 대비하여 포인터로 변경
+ PotionRecipe* FindRecipe(const std::string& name);
```

💡 포인터로 반환하는 이유

> 1. 예외 처리의 명확성 (nullptr 활용)  
> ● 기존 방식: 물약을 찾지 못했을 때 "비어있는 가짜 객체"를 억지로 생성해서 반환해야 했습니다. 이는 불필요한 메모리 사용과 복잡한 체크 로직을 야기합니다.  
> ● 개선 방식: 검색 결과가 없으면 간단히 nullptr을 반환하면 됩니다. 호출하는 쪽에서도 if (ptr == nullptr) 한 줄로 깔끔하게 예외 처리가 가능합니다.  
{: .prompt-info }

> 2. 성능 최적화 (Copy Overhead 제거)  
> ● 기존 방식: PotionRecipe 객체 내부에 많은 재료 데이터(std::vector 등)가 담겨 있을 경우, 값을 복사해서 넘겨주는 과정에서 상당한 성능 저하가 발생합니다.  
> ● 개선 방식: 객체의 크기와 상관없이 단 8바이트(64비트 기준)의 주소값만 주고받으므로 연산 속도가 비약적으로 향상됩니다.  
{: .prompt-tip }


## C++ 강의

### 1.생성자
● 클래스를 자료형으로 포인터를 선언하고 할당을 안받고 그냥 선언만 하면 생성자는 호출안된다.  
(주소값만 저장한다고 하는거지 객체가 만들어지는거는 아니다.)  

● 멤버변수로 const나 참조자가 있을 때 생성하자마자 같이 초기화를 시켜줘야하기때문에 이니셜라이즈로 무조건 초기화 시켜줘야한다.  

● 생성자가 정의되어 있는데 클래스를 배열로 받고 싶을때  
(디폴트 생성자를 만들면 되지만 못만들 경우)  

```cpp
//1번 방법
//생성자가 선언된 클래스를 배열로 선언하는 방법
int a = 10;
// 20개를 다 적기 힘들지만, 이렇게 하면 기본 생성자 없이 배열 선언이 가능합니다.
Test A[3] = { Test(a), Test(a), Test(a) };

//2번 방법
// 주소값(포인터) 20개를 담을 공간을 만듭니다. (이때는 Test의 생성자가 필요 없습니다.)
Test** arr = new Test * [20];

// 반복문을 돌며 내가 원하는 값(a)을 넣어 진짜 객체들을 하나씩 생성합니다.
for (int i = 0; i < 20; ++i)
{
		arr[i] = new Test(a); // 생성자 호출 성공!

}

//3번 방법(제일 쉽고 많이 씀)
int a = 10;
// 20개의 칸을 만들고, 모든 칸을 Test(a) 객체로 복사해서 채웁니다.
std::vector<Test> v(20, Test(a));
```
